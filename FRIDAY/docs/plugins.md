# FRIDAY Plugins

Plugins expose capabilities by registering command handlers with `CommandRegistry`.

Phase 1 plugins are dry-run only:

- `plugins/chrome`
- `plugins/files`
- `plugins/browser`
- `plugins/system`
